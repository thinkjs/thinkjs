const mocha = require('mocha');
const expect = require('chai').expect;
const muk = require('muk');
const JWTSession = require('../');

describe('JWTSession test', () => {
  it('should ok', () => {
    expect(2).to.eq(2);
  });
});
