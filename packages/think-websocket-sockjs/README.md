# think-websocket-sockjs

This module corresponds to https://github.com/thinkjs/think-websocket-sockjs.

The upstream repository is empty (no branch and no commits), so there is no history to migrate.
