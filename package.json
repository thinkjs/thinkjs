{
  "name": "thinkjs",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.6.0",
  "scripts": {
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint"
  }
}
